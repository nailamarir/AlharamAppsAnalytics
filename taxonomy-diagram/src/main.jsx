import React from 'react'
import ReactDOM from 'react-dom/client'
import TaxonomyDiagram from '../TaxonomyDiagram.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <TaxonomyDiagram />
  </React.StrictMode>
)
